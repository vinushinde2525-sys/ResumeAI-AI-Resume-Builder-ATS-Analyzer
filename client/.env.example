# Client Environment Variables
# Copy this file to .env and fill in values

# API base URL (Vite proxy handles /api in dev, set full URL for production)
VITE_API_URL=http://localhost:5000/api

# App name (used in UI)
VITE_APP_NAME=ResumeAI
